const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d
const subtracao = d - b
const multiplicacao = a * a
const divisao = d / b
const resto = 5 % 2

console.log(soma, subtracao, multiplicacao, divisao, resto)

